---
# ArgoCD Application that deploys all Temporal v1.29.4 resources
# UPGRADE NOTE: After upgrading the manifests in the repo, ArgoCD will
# automatically sync the changes. For manual sync: argocd app sync temporal
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: temporal
  namespace: argocd
  labels:
    app.kubernetes.io/name: temporal
spec:
  project: default
  source:
    repoURL: https://github.com/zsmn/zsmn-k8scd.git
    targetRevision: HEAD
    path: base
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: temporal
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true